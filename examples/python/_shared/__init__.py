"""Tiny helper package shared by the CAW Python agent examples.

Each agent script in ``examples/python`` imports from this package to avoid
repeating the handful of lines that every demo needs (environment loading,
the demo prompt, and the default list of tools exposed to the LLM).
"""
